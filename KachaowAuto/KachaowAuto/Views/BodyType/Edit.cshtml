@model KachaowAuto.ViewModels.BodyType.BodyTypeEditViewModel

@{
    ViewData["Title"] = "Edit Body Type";
}

<div class="admin-page">
    <div class="admin-header">
        <div class="admin-title-wrap">
            <h1>Edit Body Type</h1>
            <p class="admin-subtitle">
                Update the selected body type and keep the system catalog accurate.
            </p>
        </div>
    </div>

    <div class="admin-form-wrap">
        <div class="admin-form-card">
            <div class="admin-form-head">
                <h2 class="admin-form-title">Edit Body Type Details</h2>
                <p class="admin-form-subtitle">Make changes to the selected body type.</p>
            </div>

            <div class="admin-form-body">
                <form asp-action="Edit" method="post">
                    @Html.AntiForgeryToken()
                    <input type="hidden" asp-for="BodyTypeId" />

                    <div class="mb-3">
                        <label asp-for="Name" class="form-label">Body Type Name</label>
                        <input asp-for="Name" class="form-control" placeholder="Enter body type name" />
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>

                    <div class="admin-form-actions">
                        <button type="submit" class="btn btn-primary btn-admin">Save Changes</button>
                        <a asp-action="Index" class="btn btn-secondary btn-admin">Cancel</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}